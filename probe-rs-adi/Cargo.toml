[package]
name = "probe-rs-adi"
version.workspace = true
edition.workspace = true
description = "no_std ARM ADIv5 debug interface for probe-rs"
documentation.workspace = true
homepage.workspace = true
repository.workspace = true
readme.workspace = true
license.workspace = true
# Pinned to nightly-2026-04-11 (1.97.0-nightly) for const trait experiments.
# Minimum usable stable: 1.95 (when released).
rust-version = "1.95"

[dependencies]
probe-rs-wire = { path = "../probe-rs-wire" }
defmt = { version = "1.0", optional = true }

[features]
default = []
defmt = ["dep:defmt", "probe-rs-wire/defmt"]

[lints]
workspace = true
